apiVersion: v1
kind: Service
metadata:
  name: microblog-feed-api-svc
  labels:
    app: microblog-feed-api
spec:
  type: LoadBalancer
  ports:
  - port: 80
    odePort: 9101
  selector:
    env: sandbox